SEO Checklist for Henrify (Henry Ani)

- Technical
  - Verify `sitemap.xml` is reachable at `https://henrify.dev/sitemap.xml`.
  - Ensure `robots.txt` references the sitemap and allows crawling.
  - Serve valid `site.webmanifest` and favicons with correct paths.
  - Enable HTTPS and enforce it in GitHub Pages settings.

- On-page
  - Use unique `<title>` and `<meta description>` per page (done).
  - Add structured data (JSON-LD) for `Person` and `WebSite` (done).
  - Ensure heading hierarchy (H1 on each page, descriptive H2/H3).
  - Use descriptive alt text for images (brand logo, OG images).

- Content & Keywords
  - Target primary keywords: "Henry Ani", "Henrify", "Full Stack Developer", "SEO Specialist".
  - Add location or niche keywords if relevant (e.g., "web development", "SEO for startups").
  - Add blog or case studies pages to target long-tail keywords.

- Performance
  - Minimize unused CSS and JS; enable compression (GZIP/Brotli).
  - Serve images in modern formats (WebP) and use responsive `srcset`.
  - Use caching headers for static assets.

- Links & Social
  - Ensure all social `sameAs` links are correct (GitHub, X, TikTok, Instagram).
  - Add link rel="canonical" where applicable (done).

- Monitoring
  - Add site to Google Search Console and submit `sitemap.xml`.
  - Add site to Bing Webmaster Tools.
  - Configure Google Analytics for traffic monitoring.
  - Set up rank-tracking for keywords (e.g., "Henry Ani", "Henrify", "SEO Specialist").

- Next Actions
  - Submit `sitemap.xml` to Google Search Console.
  - Create 3-5 content pages (case studies / blog posts) targeting long-tail keywords.
  - Build backlink outreach plan: guest posts, link directories, GitHub projects.
